{
  "name": "polytrack-tas-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous 4-phase TAS pipeline for PolyTrack 0.6.2 (headless wasm engine + RRT* + windowed MCTS + 1ms polisher)",
  "main": "src/cli/run.js",
  "scripts": {
    "extract": "node src/extract/build-context.js",
    "test": "node --test test/",
    "bench": "node src/cli/bench.js"
  },
  "engines": {
    "node": ">=20"
  }
}
